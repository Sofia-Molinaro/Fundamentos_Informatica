# Mencione los errores en los siguientes códigos:
# a) def suma(par1, par2):
# print(par1+par2)
# suma()

# b) def suma(par1, par2):
# print (par1 + par2)
# print(suma(12, 10))

# c) def suma(par1, par2):
# return (par1 + par2)
# suma(12, 10)

# d) def suma(par1):
# return (par1 + 2)
# suma(12, 10)

#En el caso A no se retorna nada. Además, no se dieron valores a las variables par1 y par2. Entonces el programa no logra su próposito que es sumar las dos variables e imprimir el resultado por pantalla.
#En el caso B no se retorna nada. Se imprime la suma de dos variables a las que no se les ha asignado previamente un valor.  
#En el caso C el return debe ir al finalizar una función.
#En el caso D cuando se llama a la función "suma" se establecen dos parametros cuando en la primera sentencia se definió uno. Además el return debe ir al finalizar una función.
